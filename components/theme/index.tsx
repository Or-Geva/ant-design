import { ThemeProvider, ThemeContext, useTheme, ThemeType } from './context';
import ThemeToggle from './toggle';

export { ThemeProvider, ThemeContext, useTheme, ThemeToggle, ThemeType };

export default {
  ThemeProvider,
  ThemeContext,
  useTheme,
  ThemeToggle,
};
